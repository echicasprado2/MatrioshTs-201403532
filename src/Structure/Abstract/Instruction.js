class Instruction extends Node {
    /**
     * 
     * @param {*} linea 
     * @param {*} column 
     */
    constructor(linea, column) {
        super(linea, column);
    }

    /**
     * 
     * @param { Environment } e 
     */
    execute(e){
        return "implementar";
    }

}
